{
  "tasks": [
    {
      "type": "shell",
      "label": "cmake build active file",
      "command": "cmake --build ${workspaceFolder}/build --config Debug --target all -- -j 10",
      "args": [],
      "options": {
        "cwd": "/usr/bin"
      }
    }
  ],
  "version": "2.0.0"
}
